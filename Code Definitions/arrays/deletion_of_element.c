/* Deletion of Element from an Array */
// =================================================
#include<stdio.h>
void main()
{
    int arr[10] = {1,2,3,4,5,6,7,8};
    int len = 8;
    int num;

    printf("Element to be deleted : ");
    scanf("%d", &num);

    for (int i = 0; i < len; i++)
    {
        if (arr[i]==num)
            printf("")
    }
}
// ================================================
/* Code by Abel Roy */
